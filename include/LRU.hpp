#pragma once 

#include <iostream>
#include <unordered_map>

typedef int elem_type; // The type of element for which LRU cache was made

class Cache_elem
{
    public: 


    private:
        size_t num_of_calls;  // This value stores how many times element was in the flow before
        elem_type elem_value; // This value stores the value of variable
};

class LRU_cache
{
    public: 


    private: 

};  


